# Required — Claude AI analysis
ANTHROPIC_API_KEY=sk-ant-

# Optional — address autocomplete + geocoding (Google Cloud Console)
GOOGLE_PLACES_KEY=AIza

# Optional — AVM + sale history pre-fill (Rentcast — api.rentcast.io)
RENTCAST_KEY=

# Optional — Phase 3 motivated seller directory (PropStream)
PROPSTREAM_KEY=
